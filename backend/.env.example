# NovaCode Tender Intelligence — Environment Variables
# Copy this to .env and fill in your values

# Required: Your Anthropic API key for AI scoring
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional: Database path (defaults to tenders.db in current directory)
DB_PATH=tenders.db

# Optional: Port (Render sets this automatically)
PORT=8000
